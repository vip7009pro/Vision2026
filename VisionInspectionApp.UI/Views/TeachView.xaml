<UserControl x:Class="VisionInspectionApp.UI.Views.TeachView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:VisionInspectionApp.UI.Controls"
             mc:Ignorable="d">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False">
            <TextBlock Text="Config" VerticalAlignment="Center" Margin="0,0,6,0" />
            <ComboBox Width="160" ItemsSource="{Binding AvailableConfigs}" SelectedItem="{Binding SelectedConfig}" Margin="0,0,6,0" />
            <Button Content="Refresh" Command="{Binding RefreshConfigsCommand}" Margin="0,0,6,0" />
            <Button Content="Load" Command="{Binding LoadConfigCommand}" Margin="0,0,6,0" />
            <Button Content="New" Command="{Binding NewConfigCommand}" Margin="0,0,12,0" />

            <TextBlock Text="Product Code" VerticalAlignment="Center" Margin="0,0,6,0" />
            <TextBox Width="120" Text="{Binding ProductCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />

            <TextBlock Text="Target" VerticalAlignment="Center" Margin="0,0,6,0" />
            <ComboBox Width="140" ItemsSource="{Binding Targets}" SelectedItem="{Binding SelectedTarget}" Margin="0,0,12,0" />

            <TextBlock Text="ROI" VerticalAlignment="Center" Margin="0,0,6,0" />
            <ComboBox Width="110" ItemsSource="{Binding RoiModes}" SelectedItem="{Binding SelectedRoiMode}" Margin="0,0,12,0" />

            <TextBlock Text="Point" VerticalAlignment="Center" Margin="0,0,6,0" />
            <TextBox Width="120" Text="{Binding NewPointName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,6,0" />
            <Button Content="Add" Command="{Binding AddPointCommand}" Margin="0,0,12,0" />

            <Button Content="Undo" Command="{Binding UndoCommand}" Margin="0,0,6,0" />
            <Button Content="Redo" Command="{Binding RedoCommand}" Margin="0,0,12,0" />

            <Button Content="Load Image" Command="{Binding LoadImageCommand}" Margin="0,0,6,0" />
            <Button Content="Save Config" Command="{Binding SaveConfigCommand}" />
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" Margin="0,0,8,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <Border BorderBrush="#CCC" BorderThickness="1" Padding="8" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock FontWeight="Bold" Text="Preprocess" Margin="0,0,0,6" />

                            <CheckBox Content="Preview Preprocess" IsChecked="{Binding IsPreprocessPreviewEnabled}" Margin="0,0,0,6" />

                            <CheckBox Content="Gray" IsChecked="{Binding UseGray}" />

                            <CheckBox Content="Gaussian Blur" IsChecked="{Binding UseGaussianBlur}" Margin="0,6,0,0" />
                            <TextBlock Text="Blur Kernel" />
                            <Slider Minimum="1" Maximum="21" Value="{Binding BlurKernel, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="2" />

                            <CheckBox Content="Threshold" IsChecked="{Binding UseThreshold}" Margin="0,6,0,0" />
                            <TextBlock Text="Threshold Value" />
                            <Slider Minimum="0" Maximum="255" Value="{Binding ThresholdValue, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />

                            <CheckBox Content="Canny" IsChecked="{Binding UseCanny}" Margin="0,6,0,0" />
                            <TextBlock Text="Canny 1" />
                            <Slider Minimum="0" Maximum="255" Value="{Binding Canny1, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                            <TextBlock Text="Canny 2" />
                            <Slider Minimum="0" Maximum="255" Value="{Binding Canny2, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />

                            <CheckBox Content="Morphology" IsChecked="{Binding UseMorphology}" Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>

                    <Border BorderBrush="#CCC" BorderThickness="1" Padding="8" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock FontWeight="Bold" Text="Calibration" Margin="0,0,0,6" />
                            <TextBlock Text="Pixels / mm" />
                            <TextBox Text="{Binding PixelsPerMm, Converter={StaticResource DoubleConv}, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Border>

                    <TextBlock FontWeight="Bold" Text="Points" Margin="0,0,0,6" />
                    <DockPanel LastChildFill="False">
                        <Button DockPanel.Dock="Right" Content="Delete" Command="{Binding DeletePointCommand}" Margin="6,0,0,0" />
                        <ListBox Height="160" Width="220" ItemsSource="{Binding Points}" SelectedItem="{Binding SelectedPoint}" />
                    </DockPanel>

                    <TextBlock FontWeight="Bold" Text="Lines" Margin="0,8,0,6" />
                    <DockPanel LastChildFill="False">
                        <Button DockPanel.Dock="Right" Content="Delete" Command="{Binding DeleteLineToolCommand}" Margin="6,0,0,0" />
                        <ListBox Height="120" Width="220" ItemsSource="{Binding Lines}" SelectedItem="{Binding SelectedLineTool}" />
                    </DockPanel>

                    <DockPanel LastChildFill="False" Margin="0,6,0,0">
                        <TextBox Width="120" Text="{Binding NewLineToolName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,6,0" />
                        <Button Content="Add" Command="{Binding AddLineToolCommand}" />
                    </DockPanel>

                    <Border BorderBrush="#CCC" BorderThickness="1" Padding="8" Margin="0,8,0,0">
                        <StackPanel>
                            <TextBlock FontWeight="Bold" Text="Line Params" Margin="0,0,0,6" />

                            <CheckBox Content="Preview" IsChecked="{Binding IsLinePreviewEnabled}" Margin="0,0,0,6" />

                            <Border BorderBrush="#DDD" BorderThickness="1" Padding="4" Margin="0,0,0,6">
                                <Image Source="{Binding LinePreviewImage}" Stretch="Uniform" Height="120" />
                            </Border>

                            <TextBlock Text="Canny 1" />
                            <Slider Minimum="0" Maximum="255" Value="{Binding LineCanny1, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />

                            <TextBlock Text="Canny 2" Margin="0,6,0,0" />
                            <Slider Minimum="0" Maximum="255" Value="{Binding LineCanny2, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />

                            <TextBlock Text="Hough Threshold" Margin="0,6,0,0" />
                            <Slider Minimum="1" Maximum="200" Value="{Binding LineHoughThreshold, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />

                            <TextBlock Text="Min Line Length" Margin="0,6,0,0" />
                            <Slider Minimum="1" Maximum="500" Value="{Binding LineMinLineLength, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />

                            <TextBlock Text="Max Line Gap" Margin="0,6,0,0" />
                            <Slider Minimum="0" Maximum="200" Value="{Binding LineMaxLineGap, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                        </StackPanel>
                    </Border>

                    <Border BorderBrush="#CCC" BorderThickness="1" Padding="8" Margin="0,8,0,0">
                        <StackPanel>
                            <TextBlock FontWeight="Bold" Text="Distance" Margin="0,0,0,6" />

                            <TextBlock Text="Name" />
                            <TextBox Text="{Binding DistanceName, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Text="Point A" Margin="0,6,0,0" />
                            <ComboBox ItemsSource="{Binding Points}" SelectedItem="{Binding DistancePointA}" />

                            <TextBlock Text="Point B" Margin="0,6,0,0" />
                            <ComboBox ItemsSource="{Binding Points}" SelectedItem="{Binding DistancePointB}" />

                            <TextBlock Text="Nominal" Margin="0,6,0,0" />
                            <TextBox Text="{Binding DistanceNominal, Converter={StaticResource DoubleConv}, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Text="Tol +" Margin="0,6,0,0" />
                            <TextBox Text="{Binding DistanceTolPlus, Converter={StaticResource DoubleConv}, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Text="Tol -" Margin="0,6,0,0" />
                            <TextBox Text="{Binding DistanceTolMinus, Converter={StaticResource DoubleConv}, UpdateSourceTrigger=PropertyChanged}" />

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Content="Add" Command="{Binding AddDistanceCommand}" MinWidth="80" Margin="0,0,6,0" />
                                <Button Content="Update" Command="{Binding UpdateDistanceCommand}" MinWidth="80" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock FontWeight="Bold" Text="Distances" Margin="0,8,0,6" />
                    <DockPanel LastChildFill="False">
                        <Button DockPanel.Dock="Right" Content="Delete" Command="{Binding DeleteDistanceCommand}" Margin="6,0,0,0" />
                        <ListBox Height="140" Width="220" ItemsSource="{Binding Distances}" SelectedItem="{Binding SelectedDistance}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <Run Text="{Binding Name}" />
                                    <Run Text=" (" />
                                    <Run Text="{Binding PointA}" />
                                    <Run Text=" - " />
                                    <Run Text="{Binding PointB}" />
                                    <Run Text=")" />
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>

                    <Border BorderBrush="#CCC" BorderThickness="1" Padding="8" Margin="0,8,0,0">
                        <StackPanel>
                            <TextBlock FontWeight="Bold" Text="Line-Line Distance" Margin="0,0,0,6" />

                            <TextBlock Text="Name" />
                            <TextBox Text="{Binding LineToLineName, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Text="Line A" Margin="0,6,0,0" />
                            <ComboBox ItemsSource="{Binding Lines}" SelectedItem="{Binding LineToLineLineA}" />

                            <TextBlock Text="Line B" Margin="0,6,0,0" />
                            <ComboBox ItemsSource="{Binding Lines}" SelectedItem="{Binding LineToLineLineB}" />

                            <TextBlock Text="Nominal" Margin="0,6,0,0" />
                            <TextBox Text="{Binding LineToLineNominal, Converter={StaticResource DoubleConv}, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Text="Tol +" Margin="0,6,0,0" />
                            <TextBox Text="{Binding LineToLineTolPlus, Converter={StaticResource DoubleConv}, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Text="Tol -" Margin="0,6,0,0" />
                            <TextBox Text="{Binding LineToLineTolMinus, Converter={StaticResource DoubleConv}, UpdateSourceTrigger=PropertyChanged}" />

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Content="Add" Command="{Binding AddLineToLineDistanceCommand}" MinWidth="80" Margin="0,0,6,0" />
                                <Button Content="Update" Command="{Binding UpdateLineToLineDistanceCommand}" MinWidth="80" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock FontWeight="Bold" Text="Line-Line Distances" Margin="0,8,0,6" />
                    <DockPanel LastChildFill="False">
                        <Button DockPanel.Dock="Right" Content="Delete" Command="{Binding DeleteLineToLineDistanceCommand}" Margin="6,0,0,0" />
                        <ListBox Height="110" Width="220" ItemsSource="{Binding LineToLineDistances}" SelectedItem="{Binding SelectedLineToLineDistance}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                        <Run Text="{Binding Name}" />
                                        <Run Text=" (" />
                                        <Run Text="{Binding LineA}" />
                                        <Run Text=" - " />
                                        <Run Text="{Binding LineB}" />
                                        <Run Text=")" />
                                    </TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>

                    <Border BorderBrush="#CCC" BorderThickness="1" Padding="8" Margin="0,8,0,0">
                        <StackPanel>
                            <TextBlock FontWeight="Bold" Text="Point-Line Distance" Margin="0,0,0,6" />

                            <TextBlock Text="Name" />
                            <TextBox Text="{Binding PointToLineName, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Text="Point" Margin="0,6,0,0" />
                            <ComboBox ItemsSource="{Binding Points}" SelectedItem="{Binding PointToLinePoint}" />

                            <TextBlock Text="Line" Margin="0,6,0,0" />
                            <ComboBox ItemsSource="{Binding Lines}" SelectedItem="{Binding PointToLineLine}" />

                            <TextBlock Text="Nominal" Margin="0,6,0,0" />
                            <TextBox Text="{Binding PointToLineNominal, Converter={StaticResource DoubleConv}, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Text="Tol +" Margin="0,6,0,0" />
                            <TextBox Text="{Binding PointToLineTolPlus, Converter={StaticResource DoubleConv}, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Text="Tol -" Margin="0,6,0,0" />
                            <TextBox Text="{Binding PointToLineTolMinus, Converter={StaticResource DoubleConv}, UpdateSourceTrigger=PropertyChanged}" />

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Content="Add" Command="{Binding AddPointToLineDistanceCommand}" MinWidth="80" Margin="0,0,6,0" />
                                <Button Content="Update" Command="{Binding UpdatePointToLineDistanceCommand}" MinWidth="80" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock FontWeight="Bold" Text="Point-Line Distances" Margin="0,8,0,6" />
                    <DockPanel LastChildFill="False">
                        <Button DockPanel.Dock="Right" Content="Delete" Command="{Binding DeletePointToLineDistanceCommand}" Margin="6,0,0,0" />
                        <ListBox Height="110" Width="220" ItemsSource="{Binding PointToLineDistances}" SelectedItem="{Binding SelectedPointToLineDistance}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                        <Run Text="{Binding Name}" />
                                        <Run Text=" (" />
                                        <Run Text="{Binding Point}" />
                                        <Run Text=" - " />
                                        <Run Text="{Binding Line}" />
                                        <Run Text=")" />
                                    </TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>

                    <TextBlock FontWeight="Bold" Text="Last ROI" Margin="0,8,0,6" />
                    <TextBlock Text="{Binding LastRoi.X, StringFormat=X: {0}}" />
                    <TextBlock Text="{Binding LastRoi.Y, StringFormat=Y: {0}}" />
                    <TextBlock Text="{Binding LastRoi.Width, StringFormat=W: {0}}" />
                    <TextBlock Text="{Binding LastRoi.Height, StringFormat=H: {0}}" />
                </StackPanel>
            </ScrollViewer>

            <controls:ImageViewerControl Grid.Column="1"
                                        ImageSource="{Binding Image}"
                                        RoiSelectedCommand="{Binding RoiSelectedCommand}"
                                        EnableRoiEditing="True"
                                        RoiEditedCommand="{Binding RoiEditedCommand}"
                                        OverlayItems="{Binding OverlayItems}" />
        </Grid>
    </Grid>
</UserControl>
