<Window x:Class="VisionInspectionApp.UI.Views.GlobalPreprocessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:VisionInspectionApp.UI.Converters"
        Title="Global Pre-processor" Height="500" Width="360"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        ResizeMode="NoResize" Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        Topmost="True">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    
    <Grid Margin="16">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <TextBlock Text="Global Pre-processor Settings" FontWeight="Bold" FontSize="14" Margin="0,0,0,12" />

                <CheckBox Content="Live Preview (Apply to Original Image)" IsChecked="{Binding PreprocessPreviewEnabled, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" FontWeight="SemiBold" />

                <Border Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Illumination Profile" FontWeight="SemiBold" Margin="0,0,0,6" />
                        <ComboBox ItemsSource="{Binding AvailableIlluminationCorrectionPresets}" SelectedItem="{Binding IlluminationCorrection, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                        
                        <StackPanel Margin="0,0,0,8" ToolTip="Illumination Kernel Size">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                <TextBlock Text="Kernel Size" Width="100" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding IlluminationKernel}" FontWeight="SemiBold" />
                            </StackPanel>
                            <Slider Minimum="3" Maximum="401" Value="{Binding IlluminationKernel, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="2" />
                        </StackPanel>

                        <StackPanel Margin="0,0,0,8" ToolTip="CLAHE Clip Limit (Contrast Limit)">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                <TextBlock Text="CLAHE Clip" Width="100" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding ClaheClipLimit}" FontWeight="SemiBold" />
                            </StackPanel>
                            <Slider Minimum="0.1" Maximum="10" Value="{Binding ClaheClipLimit, UpdateSourceTrigger=PropertyChanged}" TickFrequency="0.1" />
                        </StackPanel>
                        
                        <StackPanel ToolTip="CLAHE Tile Grid Size">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                <TextBlock Text="CLAHE Tile" Width="100" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding ClaheTileGrid}" FontWeight="SemiBold" />
                            </StackPanel>
                            <Slider Minimum="2" Maximum="32" Value="{Binding ClaheTileGrid, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Filters" FontWeight="SemiBold" Margin="0,0,0,6" />
                        
                        <CheckBox Content="Convert to Grayscale" IsChecked="{Binding UseGray, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                        
                        <CheckBox Content="Gaussian Blur" IsChecked="{Binding UseGaussianBlur, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4" />
                        <StackPanel Margin="0,0,0,8" ToolTip="Gaussian Blur Kernel Size" IsEnabled="{Binding UseGaussianBlur}">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                <TextBlock Text="Blur Kernel" Width="100" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding BlurKernel}" FontWeight="SemiBold" />
                            </StackPanel>
                            <Slider Minimum="1" Maximum="21" Value="{Binding BlurKernel, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="2" />
                        </StackPanel>

                        <CheckBox Content="Threshold" IsChecked="{Binding UseThreshold, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4" />
                        <StackPanel Margin="0,0,0,8" ToolTip="Binarization / Local Threshold" IsEnabled="{Binding UseThreshold}">
                            <!-- ThresholdType Selection -->
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="ThresholdType:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableThresholdTypes}" SelectedItem="{Binding ThresholdType, UpdateSourceTrigger=PropertyChanged}" Margin="0"/>
                            </Grid>

                            <!-- BINARY THRESHOLD CONTROLS -->
                            <StackPanel Visibility="{Binding IsThresholdBinary, Converter={StaticResource BoolToVis}}">
                                <!-- ThresholdLow -->
                                <StackPanel Margin="0,0,0,6">
                                    <Grid Margin="0,0,0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="50" />
                                            <ColumnDefinition Width="32" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="ThresholdLow:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                        <TextBox Grid.Column="2" Text="{Binding ThresholdLow, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" Margin="0,0,4,0" VerticalContentAlignment="Center"/>
                                        <ToggleButton Grid.Column="3" IsChecked="{Binding InvertBinary, UpdateSourceTrigger=PropertyChanged}" Content="⇌" ToolTip="Invert Threshold (BinaryInv)" Padding="0" Height="22" Width="26" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Slider Minimum="0" Maximum="255" Value="{Binding ThresholdLow, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>

                                <!-- ThresholdHigh -->
                                <StackPanel Margin="0,0,0,6">
                                    <Grid Margin="0,0,0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="50" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="ThresholdHigh:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                        <TextBox Grid.Column="2" Text="{Binding ThresholdHigh, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" Margin="0,0,4,0" VerticalContentAlignment="Center"/>
                                    </Grid>
                                    <Slider Minimum="0" Maximum="255" Value="{Binding ThresholdHigh, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>
                            </StackPanel>

                            <!-- LOCAL (ADAPTIVE) THRESHOLD CONTROLS -->
                            <StackPanel Visibility="{Binding IsThresholdLocal, Converter={StaticResource BoolToVis}}">
                                <!-- MaskHeight -->
                                <StackPanel Margin="0,0,0,6">
                                    <Grid Margin="0,0,0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="50" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="MaskHeight:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                        <TextBox Grid.Column="2" Text="{Binding MaskHeight, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" Margin="0,0,4,0" VerticalContentAlignment="Center"/>
                                    </Grid>
                                    <Slider Minimum="3" Maximum="201" Value="{Binding MaskHeight, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="2" />
                                </StackPanel>

                                <!-- MaskWidth -->
                                <StackPanel Margin="0,0,0,6">
                                    <Grid Margin="0,0,0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="50" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="MaskWidth:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                        <TextBox Grid.Column="2" Text="{Binding MaskWidth, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" Margin="0,0,4,0" VerticalContentAlignment="Center"/>
                                    </Grid>
                                    <Slider Minimum="3" Maximum="201" Value="{Binding MaskWidth, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="2" />
                                </StackPanel>

                                <!-- Local Offset -->
                                <StackPanel Margin="0,0,0,6">
                                    <Grid Margin="0,0,0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="50" />
                                            <ColumnDefinition Width="32" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Local Offset:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                        <TextBox Grid.Column="2" Text="{Binding LocalOffset, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" Margin="0,0,4,0" VerticalContentAlignment="Center"/>
                                        <ToggleButton Grid.Column="3" IsChecked="{Binding InvertLocal, UpdateSourceTrigger=PropertyChanged}" Content="⇌" ToolTip="Invert Local Threshold" Padding="0" Height="22" Width="26" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Slider Minimum="-100" Maximum="100" Value="{Binding LocalOffset, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>

                        <CheckBox Content="Canny Edge" IsChecked="{Binding UseCanny, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4" />
                        <StackPanel Margin="0,0,0,4" ToolTip="Canny Threshold 1 (Lower)" IsEnabled="{Binding UseCanny}">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                <TextBlock Text="Canny Th1" Width="100" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding Canny1}" FontWeight="SemiBold" />
                            </StackPanel>
                            <Slider Minimum="0" Maximum="255" Value="{Binding Canny1, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                        </StackPanel>
                        <StackPanel Margin="0,0,0,8" ToolTip="Canny Threshold 2 (Upper)" IsEnabled="{Binding UseCanny}">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                <TextBlock Text="Canny Th2" Width="100" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding Canny2}" FontWeight="SemiBold" />
                            </StackPanel>
                            <Slider Minimum="0" Maximum="255" Value="{Binding Canny2, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                        </StackPanel>

                        <CheckBox Content="Morphology (Close &amp; Open)" IsChecked="{Binding UseMorphology, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Border>

                <Button Content="Close" Height="30" Margin="0,12,0,0" Click="CloseButton_Click" HorizontalAlignment="Right" Width="100" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>

