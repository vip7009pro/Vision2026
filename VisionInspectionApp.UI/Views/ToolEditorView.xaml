<UserControl x:Class="VisionInspectionApp.UI.Views.ToolEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:VisionInspectionApp.UI.Controls"
             mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False">
            <TextBlock Text="Config" VerticalAlignment="Center" Margin="0,0,6,0" />
            <ComboBox Width="160" ItemsSource="{Binding AvailableConfigs}" SelectedItem="{Binding SelectedConfig}" Margin="0,0,6,0" />
            <Button Content="Refresh" Command="{Binding RefreshConfigsCommand}" Margin="0,0,6,0" />
            <Button Content="Load" Command="{Binding LoadConfigCommand}" Margin="0,0,6,0" />
            <Button Content="New Graph" Command="{Binding NewGraphCommand}" Margin="0,0,12,0" />

            <Button Content="Load Image" Command="{Binding LoadPreviewImageCommand}" Margin="0,0,12,0" />

            <Button Content="Run Flow" Command="{Binding RunFlowCommand}" Margin="0,0,12,0" />

            <TextBlock Text="Product Code" VerticalAlignment="Center" Margin="0,0,6,0" />
            <TextBox Width="120" Text="{Binding ProductCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />

            <Button Content="Save" Command="{Binding SaveConfigCommand}" />
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="7*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#CCC" BorderThickness="1" Padding="8" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="2*" />
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock FontWeight="Bold" Text="Toolbox" Margin="0,0,0,6" />
                        <ListBox x:Name="ToolboxList" ItemsSource="{Binding ToolboxItems}" PreviewMouseMove="ToolboxList_PreviewMouseMove" />
                    </StackPanel>

                    <Border Grid.Row="1" BorderBrush="#EEE" BorderThickness="1,1,0,0" Padding="8" Margin="0,8,0,0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel>
                            <TextBlock FontWeight="Bold" Text="Properties" Margin="0,0,0,6" />

                            <TextBlock Text="Selected Node" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding SelectedNode.Type}" Margin="0,0,0,6" />

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="RefName" Width="60" VerticalAlignment="Center" />
                                <TextBox Width="160" Text="{Binding SelectedNode.RefName, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <TextBlock Text="Preprocess" FontWeight="SemiBold" Margin="0,8,0,6" />

                            <CheckBox Content="Preview" IsChecked="{Binding PreprocessPreviewEnabled, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

                            <CheckBox Content="Gray" IsChecked="{Binding UseGray, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,2" />
                            <CheckBox Content="Gaussian Blur" IsChecked="{Binding UseGaussianBlur, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,2" />
                            <StackPanel Margin="0,0,0,6">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Blur K" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding BlurKernel}" Width="40" />
                                </StackPanel>
                                <Slider Minimum="1" Maximum="21" Value="{Binding BlurKernel, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="2" />
                            </StackPanel>

                            <CheckBox Content="Threshold" IsChecked="{Binding UseThreshold, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,2" />
                            <StackPanel Margin="0,0,0,6">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Thr" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding ThresholdValue}" Width="40" />
                                </StackPanel>
                                <Slider Minimum="0" Maximum="255" Value="{Binding ThresholdValue, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                            </StackPanel>

                            <CheckBox Content="Canny" IsChecked="{Binding UseCanny, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,2" />
                            <StackPanel Margin="0,0,0,2">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Canny1" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Canny1}" Width="40" />
                                </StackPanel>
                                <Slider Minimum="0" Maximum="255" Value="{Binding Canny1, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,6">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Canny2" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Canny2}" Width="40" />
                                </StackPanel>
                                <Slider Minimum="0" Maximum="255" Value="{Binding Canny2, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                            </StackPanel>

                            <CheckBox Content="Morphology" IsChecked="{Binding UseMorphology, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

                            <StackPanel Visibility="{Binding IsToolWithPreprocessInput, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Tool Preprocess" FontWeight="SemiBold" Margin="0,8,0,6" />
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Pre" Width="60" VerticalAlignment="Center" />
                                    <ComboBox Width="160" ItemsSource="{Binding AvailablePreprocessChoices}" SelectedItem="{Binding SelectedToolPreprocessChoice, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsLineNode, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Line Params" FontWeight="SemiBold" Margin="0,8,0,6" />

                                <CheckBox Content="Preview" IsChecked="{Binding LinePreviewEnabled, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

                                <Border BorderBrush="#DDD" BorderThickness="1" Padding="4" Margin="0,0,0,6">
                                    <Image Source="{Binding LinePreviewImage}" Stretch="Uniform" Height="120" />
                                </Border>

                                <StackPanel Margin="0,0,0,2">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="C1" Width="60" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Line_Canny1}" Width="40" />
                                    </StackPanel>
                                    <Slider Minimum="0" Maximum="255" Value="{Binding Line_Canny1, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>

                                <StackPanel Margin="0,0,0,2">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="C2" Width="60" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Line_Canny2}" Width="40" />
                                    </StackPanel>
                                    <Slider Minimum="0" Maximum="255" Value="{Binding Line_Canny2, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>

                                <StackPanel Margin="0,0,0,2">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="Hough" Width="60" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Line_HoughThreshold}" Width="40" />
                                    </StackPanel>
                                    <Slider Minimum="1" Maximum="200" Value="{Binding Line_HoughThreshold, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>

                                <StackPanel Margin="0,0,0,2">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="MinLen" Width="60" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Line_MinLineLength}" Width="40" />
                                    </StackPanel>
                                    <Slider Minimum="1" Maximum="400" Value="{Binding Line_MinLineLength, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>

                                <StackPanel Margin="0,0,0,6">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="MaxGap" Width="60" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Line_MaxLineGap}" Width="40" />
                                    </StackPanel>
                                    <Slider Minimum="0" Maximum="100" Value="{Binding Line_MaxLineGap, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsCaliperNode, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Caliper Params" FontWeight="SemiBold" Margin="0,8,0,6" />

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Ori" Width="60" VerticalAlignment="Center" />
                                    <ComboBox Width="160" ItemsSource="{Binding AvailableCaliperOrientations}" SelectedItem="{Binding Caliper_Orientation, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Pol" Width="60" VerticalAlignment="Center" />
                                    <ComboBox Width="160" ItemsSource="{Binding AvailableEdgePolarities}" SelectedItem="{Binding Caliper_Polarity, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Strips" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Caliper_StripCount, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Width" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Caliper_StripWidth, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Length" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Caliper_StripLength, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="MinStr" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Caliper_MinEdgeStrength, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Found" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Caliper_LastRunFound, TargetNullValue='-'}" Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Avg" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Caliper_LastRunAvgStrength, TargetNullValue='-'}" Width="80" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsAnyDistanceNode, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Distance Spec" FontWeight="SemiBold" Margin="0,8,0,6" />

                                <StackPanel Visibility="{Binding IsDistanceNode, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="Inputs" FontWeight="SemiBold" Margin="0,0,0,6" />

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="A" Width="60" VerticalAlignment="Center" />
                                        <ComboBox Width="160" ItemsSource="{Binding AvailablePointNames}" SelectedItem="{Binding Distance_PointA, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <TextBlock Text="B" Width="60" VerticalAlignment="Center" />
                                        <ComboBox Width="160" ItemsSource="{Binding AvailablePointNames}" SelectedItem="{Binding Distance_PointB, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Visibility="{Binding IsLineLineDistanceNode, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="Inputs" FontWeight="SemiBold" Margin="0,0,0,6" />

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <TextBlock Text="Mode" Width="60" VerticalAlignment="Center" />
                                        <ComboBox Width="160" ItemsSource="{Binding AvailableLineLineDistanceModes}" SelectedItem="{Binding LineLineDistance_Mode, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="A" Width="60" VerticalAlignment="Center" />
                                        <ComboBox Width="160" ItemsSource="{Binding AvailableLineNames}" SelectedItem="{Binding LineLineDistance_LineA, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <TextBlock Text="B" Width="60" VerticalAlignment="Center" />
                                        <ComboBox Width="160" ItemsSource="{Binding AvailableLineNames}" SelectedItem="{Binding LineLineDistance_LineB, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Visibility="{Binding IsPointLineDistanceNode, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="Inputs" FontWeight="SemiBold" Margin="0,0,0,6" />

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <TextBlock Text="Mode" Width="60" VerticalAlignment="Center" />
                                        <ComboBox Width="160" ItemsSource="{Binding AvailablePointLineDistanceModes}" SelectedItem="{Binding PointLineDistance_Mode, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="P" Width="60" VerticalAlignment="Center" />
                                        <ComboBox Width="160" ItemsSource="{Binding AvailablePointNames}" SelectedItem="{Binding PointLineDistance_Point, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <TextBlock Text="L" Width="60" VerticalAlignment="Center" />
                                        <ComboBox Width="160" ItemsSource="{Binding AvailableLineNames}" SelectedItem="{Binding PointLineDistance_Line, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Visibility="{Binding IsAngleNode, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="Inputs" FontWeight="SemiBold" Margin="0,0,0,6" />

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="A" Width="60" VerticalAlignment="Center" />
                                        <ComboBox Width="160" ItemsSource="{Binding AvailableLineNames}" SelectedItem="{Binding Angle_LineA, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <TextBlock Text="B" Width="60" VerticalAlignment="Center" />
                                        <ComboBox Width="160" ItemsSource="{Binding AvailableLineNames}" SelectedItem="{Binding Angle_LineB, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Nom" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Distance_Nominal, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Tol+" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Distance_TolPlus, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Tol-" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Distance_TolMinus, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Value" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding SelectedRunValue, TargetNullValue='-'}" Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Pass" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding SelectedRunPass, TargetNullValue='-'}" Width="80" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsConditionNode, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Condition Params" FontWeight="SemiBold" Margin="0,8,0,6" />

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Inputs" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="60" Text="{Binding Condition_InputCount, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>

                                <TextBlock Text="Expression" Margin="0,0,0,2" />
                                <TextBox Text="{Binding Condition_Expression, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="80" TextWrapping="Wrap" />
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsBlobDetectionNode, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Blob Detection" FontWeight="SemiBold" Margin="0,8,0,6" />

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Polarity" Width="60" VerticalAlignment="Center" />
                                    <ComboBox Width="160" ItemsSource="{Binding AvailableBlobPolarities}" SelectedItem="{Binding Blob_Polarity, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Thr" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Blob_Threshold}" Width="40" />
                                </StackPanel>
                                <Slider Minimum="0" Maximum="255" Value="{Binding Blob_Threshold, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" Margin="0,0,0,6" />

                                <Border BorderBrush="#CCC" BorderThickness="1" Padding="2" Margin="0,0,0,6">
                                    <Image Source="{Binding BlobThresholdPreviewImage}" Stretch="Uniform" Height="120" />
                                </Border>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="MinA" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Blob_MinBlobArea, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="MaxA" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Blob_MaxBlobArea, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Count" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Blob_LastRunCount, TargetNullValue='-'}" Width="80" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsLinePairDetectionNode, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Line Pair Detection" FontWeight="SemiBold" Margin="0,8,0,6" />

                                <StackPanel Margin="0,0,0,2">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="C1" Width="60" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Lpd_Canny1}" Width="40" />
                                    </StackPanel>
                                    <Slider Minimum="0" Maximum="255" Value="{Binding Lpd_Canny1, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>

                                <StackPanel Margin="0,0,0,2">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="C2" Width="60" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Lpd_Canny2}" Width="40" />
                                    </StackPanel>
                                    <Slider Minimum="0" Maximum="255" Value="{Binding Lpd_Canny2, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>

                                <StackPanel Margin="0,0,0,2">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="Hough" Width="60" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Lpd_HoughThreshold}" Width="40" />
                                    </StackPanel>
                                    <Slider Minimum="1" Maximum="200" Value="{Binding Lpd_HoughThreshold, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>

                                <StackPanel Margin="0,0,0,2">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="MinLen" Width="60" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Lpd_MinLineLength}" Width="40" />
                                    </StackPanel>
                                    <Slider Minimum="1" Maximum="400" Value="{Binding Lpd_MinLineLength, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>

                                <StackPanel Margin="0,0,0,6">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                        <TextBlock Text="MaxGap" Width="60" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Lpd_MaxLineGap}" Width="40" />
                                    </StackPanel>
                                    <Slider Minimum="0" Maximum="100" Value="{Binding Lpd_MaxLineGap, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="True" TickFrequency="1" />
                                </StackPanel>

                                <TextBlock Text="Spec" FontWeight="SemiBold" Margin="0,6,0,6" />
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Nom" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Distance_Nominal, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Tol+" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Distance_TolPlus, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Tol-" Width="60" VerticalAlignment="Center" />
                                    <TextBox Width="80" Text="{Binding Distance_TolMinus, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Value" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding SelectedRunValue, TargetNullValue='-'}" Width="120" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Pass" Width="60" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding SelectedRunPass, TargetNullValue='-'}" Width="120" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsCodeDetectionNode, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Code Detection" FontWeight="SemiBold" Margin="0,8,0,6" />

                                <CheckBox Content="Try Harder" IsChecked="{Binding Cdt_TryHarder, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

                                <TextBlock Text="Symbologies" FontWeight="SemiBold" Margin="0,0,0,6" />
                                <CheckBox Content="QR" IsChecked="{Binding Cdt_EnableQr, UpdateSourceTrigger=PropertyChanged}" />
                                <CheckBox Content="Barcode 1D" IsChecked="{Binding Cdt_EnableBarcode1D, UpdateSourceTrigger=PropertyChanged}" />
                                <CheckBox Content="DataMatrix" IsChecked="{Binding Cdt_EnableDataMatrix, UpdateSourceTrigger=PropertyChanged}" />
                                <CheckBox Content="PDF417" IsChecked="{Binding Cdt_EnablePdf417, UpdateSourceTrigger=PropertyChanged}" />
                                <CheckBox Content="Aztec" IsChecked="{Binding Cdt_EnableAztec, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Text" Width="60" VerticalAlignment="Top" />
                                    <TextBlock Text="{Binding SelectedRunText, TargetNullValue='-'}" TextWrapping="Wrap" Width="160" />
                                </StackPanel>
                            </StackPanel>

                            <Button Content="Delete Node" Command="{Binding DeleteSelectedNodeCommand}" />
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" BorderBrush="#CCC" BorderThickness="1" Padding="8" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock FontWeight="Bold" Text="Canvas" Margin="0,0,0,6" />

                    <ScrollViewer x:Name="CanvasScrollViewer" Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Canvas x:Name="EditorCanvas" Background="#11111111" Width="2000" Height="2000" AllowDrop="True" Drop="EditorCanvas_Drop" MouseMove="EditorCanvas_MouseMove" MouseLeftButtonUp="EditorCanvas_MouseLeftButtonUp"
                                Focusable="True" PreviewKeyDown="EditorCanvas_PreviewKeyDown" PreviewMouseWheel="EditorCanvas_PreviewMouseWheel">
                            <Canvas.LayoutTransform>
                                <ScaleTransform ScaleX="{Binding CanvasZoom}" ScaleY="{Binding CanvasZoom}" />
                            </Canvas.LayoutTransform>

                            <ItemsControl ItemsSource="{Binding Edges}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Path Data="{Binding PathData}" Stroke="#2E7DFF" StrokeThickness="2"
                                              MouseLeftButtonDown="Edge_MouseLeftButtonDown" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Path x:Name="WirePreviewPath" Stroke="#FF9800" StrokeThickness="2" Visibility="Collapsed" />

                            <Rectangle x:Name="SelectionRect" Visibility="Collapsed" IsHitTestVisible="False"
                                       Stroke="#FF9800" StrokeThickness="1" StrokeDashArray="4 2"
                                       Fill="#33FF9800" />

                            <ItemsControl ItemsSource="{Binding Nodes}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Thumb DragStarted="NodeThumb_DragStarted" DragDelta="NodeThumb_DragDelta" DragCompleted="NodeThumb_DragCompleted" MouseLeftButtonDown="NodeThumb_MouseLeftButtonDown">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Grid Width="160" Height="{Binding NodeHeight}">
                                                        <Border CornerRadius="4" Padding="8" Margin="0">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="BorderBrush" Value="#666" />
                                                                    <Setter Property="BorderThickness" Value="1" />
                                                                    <Setter Property="Background" Value="#FFF" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                            <Setter Property="BorderBrush" Value="#FF9800" />
                                                                            <Setter Property="BorderThickness" Value="2" />
                                                                            <Setter Property="Background" Value="#FFF8E1" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                            <DockPanel LastChildFill="True">
                                                                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,6">
                                                                    <TextBlock FontWeight="Bold" Text="{Binding Type}" />
                                                                    <TextBlock Text="{Binding RefName}" Foreground="#555" />
                                                                </StackPanel>

                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*" />
                                                                        <ColumnDefinition Width="*" />
                                                                    </Grid.ColumnDefinitions>

                                                                    <ItemsControl Grid.Column="0" ItemsSource="{Binding InPorts}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-6,0,0,0">
                                                                        <ItemsControl.ItemsPanel>
                                                                            <ItemsPanelTemplate>
                                                                                <StackPanel Orientation="Vertical" />
                                                                            </ItemsPanelTemplate>
                                                                        </ItemsControl.ItemsPanel>
                                                                        <ItemsControl.ItemTemplate>
                                                                            <DataTemplate>
                                                                                <Grid Tag="{Binding Tag}" Margin="0,4,0,4">
                                                                                    <Grid.ColumnDefinitions>
                                                                                        <ColumnDefinition Width="12" />
                                                                                        <ColumnDefinition Width="Auto" />
                                                                                    </Grid.ColumnDefinitions>
                                                                                    <Ellipse Width="12" Height="12" Fill="#2ECC71" Stroke="#1B5E20" StrokeThickness="1" />
                                                                                    <TextBlock Grid.Column="1" Text="{Binding Name}" Margin="6,0,0,0" VerticalAlignment="Center" Foreground="#333" FontSize="11" />
                                                                                </Grid>
                                                                            </DataTemplate>
                                                                        </ItemsControl.ItemTemplate>
                                                                    </ItemsControl>

                                                                    <ItemsControl Grid.Column="1" ItemsSource="{Binding OutPorts}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,-6,0">
                                                                        <ItemsControl.ItemsPanel>
                                                                            <ItemsPanelTemplate>
                                                                                <StackPanel Orientation="Vertical" />
                                                                            </ItemsPanelTemplate>
                                                                        </ItemsControl.ItemsPanel>
                                                                        <ItemsControl.ItemTemplate>
                                                                            <DataTemplate>
                                                                                <Ellipse Width="12" Height="12" Fill="#2E7DFF" Stroke="#0D47A1" StrokeThickness="1"
                                                                                         Tag="{Binding Tag}" Margin="0,4,0,4"
                                                                                         MouseLeftButtonDown="OutPort_MouseLeftButtonDown" />
                                                                            </DataTemplate>
                                                                        </ItemsControl.ItemTemplate>
                                                                    </ItemsControl>
                                                                </Grid>
                                                            </DockPanel>
                                                        </Border>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Canvas>
                    </ScrollViewer>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="2" Width="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#DDD" />

            <Border Grid.Column="3" BorderBrush="#CCC" BorderThickness="1" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock FontWeight="Bold" Text="Preview - Selected Node" Margin="0,0,0,6" />
                    <CheckBox Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Center" Content="Show ROI" IsChecked="{Binding ShowRoisInSelectedPreview, UpdateSourceTrigger=PropertyChanged}" />
                    <Border Grid.Row="1" BorderBrush="#EEE" BorderThickness="1" Background="#11111111">
                        <controls:ImageViewerControl ImageSource="{Binding SelectedNodePreviewImage}"
                                                  OverlayItems="{Binding SelectedNodeOverlayItems}"
                                                  EnableRoiEditing="True"
                                                  RoiSelectedCommand="{Binding RoiSelectedCommand}"
                                                  RoiEditedCommand="{Binding RoiEditedCommand}"
                                                  RoiDeletedCommand="{Binding RoiDeletedCommand}" />
                    </Border>

                    <TextBlock Grid.Row="2" FontWeight="Bold" Text="Preview - Final Output" Margin="0,10,0,6" />
                    <CheckBox Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,10,0,6" Content="Show ROI" IsChecked="{Binding ShowRoisInFinalPreview, UpdateSourceTrigger=PropertyChanged}" />
                    <Border Grid.Row="3" BorderBrush="#EEE" BorderThickness="1" Background="#11111111">
                        <controls:ImageViewerControl ImageSource="{Binding FinalPreviewImage}" OverlayItems="{Binding FinalOverlayItems}" />
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
