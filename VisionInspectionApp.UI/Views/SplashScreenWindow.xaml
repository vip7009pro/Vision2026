<Window x:Class="VisionInspectionApp.UI.Views.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vision Inspection System 2026"
        Height="310" Width="530"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False">

    <Window.Resources>
        <!-- Fade in animation -->
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25" />
        </Storyboard>
        <!-- Fade out animation -->
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.3" />
        </Storyboard>
    </Window.Resources>

    <!-- Outer shadow container -->
    <Border Margin="14" CornerRadius="10" Background="#0F172A" BorderBrush="#0284C7" BorderThickness="1.5">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="25" ShadowDepth="6" Opacity="0.75" />
        </Border.Effect>

        <Grid ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="4" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Top Colorful Accent Strip -->
            <Border Grid.Row="0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#00E5FF" Offset="0.0" />
                        <GradientStop Color="#0284C7" Offset="0.5" />
                        <GradientStop Color="#6366F1" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <!-- Top Header: Badge & Version -->
            <DockPanel Grid.Row="1" Margin="22,14,22,6">
                <Border Background="#1E293B" BorderBrush="#38BDF8" BorderThickness="1" CornerRadius="4" Padding="8,3" HorizontalAlignment="Left">
                    <TextBlock Text="AI INDUSTRIAL VISION &amp; OQC" FontSize="9.5" FontWeight="Bold" Foreground="#38BDF8" />
                </Border>
                <TextBlock Text="v2026.8 Enterprise" FontSize="10.5" FontWeight="SemiBold" Foreground="#94A3B8" HorizontalAlignment="Right" VerticalAlignment="Center" />
            </DockPanel>

            <!-- Center Content: Logo & Title -->
            <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20,4,20,10">
                <!-- Glowing Camera/Target Icon -->
                <Border Width="52" Height="52" CornerRadius="12" Background="#1E293B" BorderBrush="#0284C7" BorderThickness="1.5" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Border.Effect>
                        <DropShadowEffect Color="#00E5FF" BlurRadius="12" ShadowDepth="0" Opacity="0.4" />
                    </Border.Effect>
                    <TextBlock Text="🎯" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>

                <!-- Solid Crisp White Title -->
                <TextBlock Text="VISION INSPECTION SYSTEM" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" />

                <!-- Subtitle -->
                <TextBlock Text="Hệ Thống Kiểm Tra Thị Giác &amp; Đo Lường Tự Động OQC" FontSize="11.5" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,4,0,0" />
            </StackPanel>

            <!-- Bottom Progress Card (Solid High-Contrast Container) -->
            <Border Grid.Row="3" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="6" Margin="22,0,22,10" Padding="12,9">
                <StackPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock x:Name="TxtStatus" Text="Đang khởi tạo các thành phần hệ thống..." FontSize="11.5" Foreground="#38BDF8" FontWeight="SemiBold" />
                        <TextBlock x:Name="TxtPercentage" Text="15%" FontSize="11.5" Foreground="#F8FAFC" HorizontalAlignment="Right" FontWeight="Bold" />
                    </DockPanel>

                    <!-- High-contrast Progress Bar -->
                    <Border Height="5.5" CornerRadius="3" Background="#0F172A" BorderBrush="#334155" BorderThickness="0.5">
                        <ProgressBar x:Name="PrgLoading" Height="5" Minimum="0" Maximum="100" Value="15" BorderThickness="0" Background="Transparent">
                            <ProgressBar.Resources>
                                <Style TargetType="ProgressBar">
                                    <Setter Property="Foreground">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#0284C7" Offset="0.0" />
                                                <GradientStop Color="#00E5FF" Offset="1.0" />
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ProgressBar.Resources>
                        </ProgressBar>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Footer info -->
            <DockPanel Grid.Row="4" Margin="24,0,24,12">
                <TextBlock Text="Vision Core Engine • .NET 8 WPF • OpenCvSharp" FontSize="9.5" Foreground="#64748B" VerticalAlignment="Center" />
                <TextBlock Text="● Ready" FontSize="9.5" Foreground="#10B981" HorizontalAlignment="Right" FontWeight="Bold" VerticalAlignment="Center" />
            </DockPanel>
        </Grid>
    </Border>
</Window>
