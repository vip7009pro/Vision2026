<Window x:Class="VisionInspectionApp.UI.Views.OriginTrainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:VisionInspectionApp.UI.Controls"
        mc:Ignorable="d"
        Title="Train Template" Height="740" Width="1200"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E" Foreground="White"
        UseLayoutRounding="True">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#434346"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="1.5*" />
            </Grid.ColumnDefinitions>

            <!-- LEFT PANEL: Training Template -->
            <Border Grid.Column="0" BorderBrush="#333337" BorderThickness="1" Background="#252526" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Training Template" FontWeight="Bold" FontSize="14" Margin="0,0,0,8" Foreground="#007ACC" />

                    <!-- Toolbar -->
                    <WrapPanel Grid.Row="1" Margin="0,0,0,8" VerticalAlignment="Center">
                        <CheckBox Content="Eraser" IsChecked="{Binding IsEraserActive}" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <TextBlock Text="Size:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBox Text="{Binding EraserBrushSize, UpdateSourceTrigger=PropertyChanged}" Width="45" Margin="0,0,12,0" TextAlignment="Center"/>
                        <Button Content="Undo" Command="{Binding UndoCommand}" Margin="0,0,6,0" />
                        <Button Content="Redo" Command="{Binding RedoCommand}" Margin="0,0,12,0" />
                        <Button Content="🎯 Fit View" Click="OnFitViewClicked" Margin="0,0,6,0" ToolTip="Cân chỉnh toàn bộ ảnh vừa khít khung nhìn" />
                        <Button Content="Current Image" Command="{Binding RefreshCurrentImageCommand}" Margin="0,0,6,0" />
                        <Button Content="Train" Command="{Binding TrainCommand}" Background="#007ACC" Foreground="White" FontWeight="SemiBold"/>
                    </WrapPanel>

                    <!-- Preview Container with ImageViewerControl (Full handles: 8 resize handles + rotation handle) -->
                    <Border Grid.Row="2" BorderBrush="#434346" BorderThickness="1" Background="#111111" ClipToBounds="True">
                        <controls:ImageViewerControl x:Name="OriginImageViewer"
                                                 ImageSource="{Binding FullPreviewImage}"
                                                 OverlayItems="{Binding OverlayItems}"
                                                 EnableRoiEditing="True"
                                                 RoiEditedCommand="{Binding RoiEditedCommand}"/>
                    </Border>
                </Grid>
            </Border>

            <!-- MIDDLE PANEL: Generated Template -->
            <Border Grid.Column="2" BorderBrush="#333337" BorderThickness="1" Background="#252526" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,8">
                        <Button Content="Save" Command="{Binding SaveCommand}" DockPanel.Dock="Right" Padding="12,2" Background="#0E639C"/>
                        <TextBlock Text="Generated Template" FontWeight="Bold" FontSize="13" VerticalAlignment="Center" Foreground="#007ACC"/>
                    </DockPanel>

                    <Border Grid.Row="2" BorderBrush="#434346" BorderThickness="1" Background="#111111" Margin="0,4,0,0">
                        <Image Source="{Binding GeneratedTemplateImage}" Stretch="Uniform"/>
                    </Border>
                </Grid>
            </Border>

            <!-- RIGHT PANEL: Parameter Configuration & Shape Operations -->
            <Grid Grid.Column="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1.4*" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>

                <!-- Parameter Configuration -->
                <Border Grid.Row="0" BorderBrush="#333337" BorderThickness="1" Background="#252526" Padding="10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Parameter Configuration" FontWeight="Bold" FontSize="13" Margin="0,0,0,10" Foreground="#007ACC"/>

                            <CheckBox Content="Auto Thresh" IsChecked="{Binding AutoThresh}" Margin="0,0,0,8"/>

                            <TextBlock Text="Edge Threshold" Margin="0,4,0,2"/>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                </Grid.ColumnDefinitions>
                                <Slider Minimum="1" Maximum="255" Value="{Binding EdgeThreshold}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBox Grid.Column="1" Text="{Binding EdgeThreshold, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center"/>
                            </Grid>

                            <TextBlock Text="Length Threshold" Margin="0,4,0,2"/>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                </Grid.ColumnDefinitions>
                                <Slider Minimum="1" Maximum="100" Value="{Binding LengthThreshold}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBox Grid.Column="1" Text="{Binding LengthThreshold, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center"/>
                            </Grid>

                            <Grid Margin="0,4,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Max Pyramid Layer Number:" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding MaxPyramidLayers}" TextAlignment="Center"/>
                            </Grid>

                            <Border BorderBrush="#333337" BorderThickness="0,1,0,0" Margin="0,4,0,8" Padding="0,8,0,0">
                                <StackPanel>
                                    <DockPanel Margin="0,0,0,6">
                                        <CheckBox Content="Lock" IsChecked="{Binding LockOriginCenter}" DockPanel.Dock="Right" VerticalAlignment="Center"/>
                                        <TextBlock Text="Origin Center" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </DockPanel>

                                    <Grid Margin="0,2,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="70" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="OriginX:" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding OriginX, StringFormat={}{0:F3}}" IsReadOnly="{Binding LockOriginCenter}"/>
                                    </Grid>

                                    <Grid Margin="0,2,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="70" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="OriginY:" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding OriginY, StringFormat={}{0:F3}}" IsReadOnly="{Binding LockOriginCenter}"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <!-- Detection ROI & Shape Operation List Table -->
                <Border Grid.Row="2" BorderBrush="#333337" BorderThickness="1" Background="#252526" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Text="Detection Roi" FontWeight="Bold" FontSize="13" Margin="0,0,0,6" Foreground="#007ACC"/>

                        <WrapPanel Grid.Row="1" Margin="0,0,0,8">
                            <RadioButton Content="Full graph" IsChecked="{Binding IsFullGraph}" Margin="0,0,16,0"/>
                            <RadioButton Content="Part graph" IsChecked="{Binding IsPartGraph}"/>
                        </WrapPanel>

                        <!-- Shape Operations List Table -->
                        <Border Grid.Row="2" BorderBrush="#434346" BorderThickness="1" Background="#1B1B1C">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="24" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Background="#2D2D30">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40" />
                                        <ColumnDefinition Width="60" />
                                        <ColumnDefinition Width="110" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="No." Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="11" FontWeight="Bold"/>
                                    <TextBlock Text="Shape" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="11" FontWeight="Bold"/>
                                    <TextBlock Text="Operation type" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="11" FontWeight="Bold"/>
                                    <TextBlock Text="Operation" Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="11" FontWeight="Bold"/>
                                </Grid>

                                <Grid Grid.Row="1" Margin="4,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40" />
                                        <ColumnDefinition Width="60" />
                                        <ColumnDefinition Width="110" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Rect" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#007ACC"/>
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <RadioButton Content="Add" IsChecked="True" FontSize="10" Margin="0,0,4,0"/>
                                        <RadioButton Content="Deduct" FontSize="10"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <Hyperlink Foreground="#007ACC">ParamSet</Hyperlink>
                                    </TextBlock>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- FOOTER: OK & Cancel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Command="{Binding OkCommand}" Width="90" Margin="0,0,10,0" Background="#007ACC" Foreground="White" FontWeight="Bold"/>
            <Button Content="Cancel" Click="CancelButton_Click" Width="90" Background="#3E3E42"/>
        </StackPanel>
    </Grid>
</Window>
